[package]
name = "byline-contract"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = "20.0"
soroban-sdk-macros = "20.0"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
